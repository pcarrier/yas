import { LRParser } from "@lezer/lr";

/** Generated from syntax.grammar by @lezer/generator (see vite.config.ts).
 *  Typed as LRParser, not Parser, so `.configure({props})` typechecks. */
export declare const parser: LRParser;
