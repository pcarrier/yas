# Tools needed a place to meet

- Publish a watched UTF-8 name with one session listener, generation, and metadata.
- Connect/accept opens a bidirectional message Transfer with exact boundaries and credit.
- Generation checks stop raced commands from landing on a replacement listener.
- Existing conversations survive listener close; Transfer close/reset ends them.
- Put RPC, streaming results, actor mailboxes, or command schemas in the messages.
- Deliberately not built-in pub/sub—and never a terminal substitute.
