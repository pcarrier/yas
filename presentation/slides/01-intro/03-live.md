# The server stays. Clients come and go.

- Watch only the terminal, window, repository, or process you care about now.
- Join from a browser, CLI, extension, or agent; each gets the same current state.
- Reconnect with a bounded snapshot, then receive only what changes.
- Retry safely, recover explicitly, and know exactly what gets cleaned up.
