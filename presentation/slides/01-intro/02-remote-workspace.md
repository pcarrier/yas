# A shell stopped being enough

- I still reach for a shell first. Then I need files, Git, language servers, processes, windows, media, and networks.
- Tunneling every tool separately means separate identity, lifecycle, recovery—and failure.
- A transcript cannot restore live state; one slow client should never slow everyone else.
