# Files without a mount

- Open a platform path, terminal/process CWD, single file, or per-session drag staging root.
- Keep paths safe and exact: component vectors, Unix bytes, Windows model, no hidden traversal normalization.
- Watch snapshot + add/replace/patch/move/remove with metadata, BLAKE3, inline threshold, lazy content.
- Read/stat/hash/link/content; ranked search, index, grep; typed pages and cursors.
- Respect ignore rules, hidden/Git controls, rename identity, and watcher reset.
- Commit staged, hashed, atomic writes—or batch write/mkdir/remove/rename/symlink/hardlink with preconditions.
