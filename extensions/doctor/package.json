{
  "name": "@yas-run/extension-doctor",
  "private": true,
  "type": "module",
  "description": "Check a YAS server, the extension runtime, and advertised capabilities",
  "scripts": {
    "build": "bun build src/main.ts --target=browser --format=esm --minify --outfile=../dist/doctor.js",
    "typecheck": "tsc --project ../tsconfig.json",
    "test": "bun test src ../typescript"
  }
}
